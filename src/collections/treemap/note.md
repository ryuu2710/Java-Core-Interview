NavigableMap API: Sức mạnh thực sự của TreeMap nằm ở interface NavigableMap. Hãy học thuộc các hàm:

- lowerEntry(k): Nhỏ hơn hẳn k.

- floorEntry(k): Nhỏ hơn hoặc bằng k.

- ceilingEntry(k): Lớn hơn hoặc bằng k.

- higherEntry(k): Lớn hơn hẳn k.